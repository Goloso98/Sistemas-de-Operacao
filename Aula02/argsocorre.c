#include <stdio.h>
#include <string.h>
#include <stdlib.h>

#define MAX_STR_SIZE 64

int ocorre(char* p1, char* p2) {
   int ocorre;
   int i;

   
}

int main(int argc, char const *argv[])
{
   char* p1 = (char*)malloc(MAX_STR_SIZE);
   char* p2 = (char*)malloc(MAX_STR_SIZE);

   printf("%d\n", ocorre(p1, p2));

   return 0;
}